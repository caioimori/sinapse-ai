# DS Guidelines (fixture)

## Rule 01
Use semantic tokens over primitives.

## Rule 02
Headlines never in dead-zone (32-48px).

## Rule 03
Layout fluido com clamp() — nunca max-w-7xl em container principal.
